apiVersion: apps/v1
kind: Deployment
metadata:
  name: test-max-replicas-violation
  namespace: demo
spec:
  replicas: 6
  selector:
    matchLabels:
      app: test-max-replicas-violation
  template:
    metadata:
      labels:
        app: test-max-replicas-violation
    spec:
      containers:
        - name: nginx
          image: nginx:1.21
          resources:
            limits:
              cpu: 100m
              memory: 128Mi
          securityContext:
            runAsUser: 1000
